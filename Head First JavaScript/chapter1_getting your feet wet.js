setTimeout(wakeUpUser, 5000);

function wakeUpUser() {
    alert('Are you going to stare at this boring page foreever?');
}

let winners = 2;
let name = "Duke";
let isEligible = false;
console.log(isEligible);

let losers;

// Test for jokes
let joke = "JavaScript walked into the bar...";
let toldjoke = "false";
let toldjoke = "false";
let $punchline = "Better watchout for those semi-colons."
// let %entage = 20; Wrong
let result

if (toldjoke == true) {
    alert($punchline);
} else {
    alert(joke);
}

//Move Night
// let zip code = 98104; Wrong
// let joe'sfavorateMovie = forbidden Planet; Wrong
let movieTicket$ = 9;
if (movieTicket$ >= 9) {
    alert("Too much");
} else {
    //  alert("We're going to see " + joe's favorateMovie); Wrong
}


var price = 28.99;
var discount = 10;
var total = (price * (discount * 100));

if (total > 25) {
    freeshipping();
}

var count = 10;
while (count > 0) {
    juggle();
}

var dog = { name: "Rover", weight: 35 };
if (dog.weight > 30) {
    alert("WOOF WOOF");
} else {
    alert("woof woof");
}

var circleRadius = 20;
var circleArea = Math.PI * (circle * circleRadius);

// If statement

let age = 25;
let name = "owen";

if (age > 14) {
    alert("Sorry this page is for kids only!");
} else {
    alert("welcome " + name + "!");
}

// Express Yourself

let total = price - (price * (discount / 100));

/*
total = variable
'=' - assignment
'*' - multiply
price - (price * (discount / 100)) - expression;
*/

// Sharpen your pencil


console.log((9 / 5) * temp + 32)

color === "orange";
name + ", " + "You've won!"

yourlevel > 5

    (levels * points) + bonus;

color != "orange"
// "!" operator tests if two values are not equal to each other

// Doing things more than once
//  while loop
while (scoops > 0) {
    document.write("Another scoop!");
    scoops = scoops - 1;
}

//Making Decisions with javascript
//If statement
if (scoops >= 50) {
    alert("Icecream is running low")
}
// String together multiple tests
if (scoops >= 50) {
    alert('Eat faster, the icecream is going to melt')
} else if (scoops < 3) {
    alert("Icecream is running low");
}

//When we need to make a lot of decisions
if (scoops >= 50) {
    alert('Eat faster, the icecream is going to melt')
} else if (scoops < 3) {
    alert("Icecream is running low");
}

// How While loop works
let scoops = 5;
while (scoops > 0) {
    document.write("Another scoop!<br>");
    scoops = scoops - 1;

}
document.write("Life without ice cream isn't the same");
